import styled from 'styled-components';

// Negative margin is for visual balance
export const TooltipBoxStyled = styled.span`
  vertical-align: bottom;
  margin-top: -1px;
  display: inline-flex;
`;

TooltipBoxStyled.displayName = 'TooltipBoxStyled';
