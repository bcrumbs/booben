import styled from 'styled-components';
import { baseModule } from '../../../../styles/themeSelectors';

export const ToggleBoxStyled = styled.div`
  margin-right: ${baseModule(0.5)}px;
`;

ToggleBoxStyled.displayName = 'ToggleBoxStyled';
