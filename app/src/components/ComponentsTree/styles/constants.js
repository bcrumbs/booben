'use strict';

import { baseModule } from '../../../styles/themeSelectors';

export default {
  item: {
    minWIdth: 100,
    maxWidth: 300,
    paddingX: baseModule(1),
    paddingY: baseModule(0.5),
  },
};
