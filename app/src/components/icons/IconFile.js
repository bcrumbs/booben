import React from 'react';
import { IconSvg } from '@reactackle/reactackle';
import { defaultProps } from './index';

export const IconFile = props => (
  <IconSvg {...props} />
);

IconFile.defaultProps = defaultProps;
