import styled from 'styled-components';
import { ArrpwStyled } from './ArrowStyled';

export const PathStyled = styled.path`  
  pointer-events: initial;
  fill: none;
`;

PathStyled.displayName = 'PathStyled';
