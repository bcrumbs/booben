import styled from 'styled-components';
import constants from '../../styles/constants';

export const TextBoxStyled = styled.div`
  flex-grow: 1;
  text-align: left;
  min-width: 0;
  padding: ${constants.title.paddingY}px;
`;
