'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

export const ActionTitleTextStyled = styled.span`
  margin-right: 0.5em;
`;

ActionTitleTextStyled.propTypes = propTypes;
ActionTitleTextStyled.defaultProps = defaultProps;
ActionTitleTextStyled.displayName = 'ActionTitleTextStyled';
