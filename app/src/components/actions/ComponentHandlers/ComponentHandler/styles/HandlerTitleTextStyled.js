'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

export const HandlerTitleTextStyled = styled.span`
  margin-right: 0.5em;
`;

HandlerTitleTextStyled.propTypes = propTypes;
HandlerTitleTextStyled.defaultProps = defaultProps;
HandlerTitleTextStyled.displayName = 'HandlerTitleTextStyled';
