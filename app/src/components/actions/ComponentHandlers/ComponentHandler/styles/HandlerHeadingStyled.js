'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

export const HandlerHeadingStyled = styled.div`
  display: flex;
  flex-grow: 1;
  user-select: none;
`;

HandlerHeadingStyled.propTypes = propTypes;
HandlerHeadingStyled.defaultProps = defaultProps;
HandlerHeadingStyled.displayName = 'HandlerHeadingStyled';
