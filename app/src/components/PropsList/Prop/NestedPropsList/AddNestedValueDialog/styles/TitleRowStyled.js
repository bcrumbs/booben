'use strict';

import styled from 'styled-components';

import {
  fontSizeBody2,
  fontWeightSemibold,
} from '../../../../../../styles/themeSelectors';

export const TitleRowStyled = styled.div`
  font-size: ${fontSizeBody2}px;
  font-weight: ${fontWeightSemibold};
`;

TitleRowStyled.displayName = 'TitleRowStyled';
