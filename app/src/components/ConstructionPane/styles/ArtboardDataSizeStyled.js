import styled from 'styled-components';
import { baseModule } from '../../../styles/themeSelectors';

export const ArtboardDataSizeStyled = styled.div`
  flex-shrink: 0;
  margin-left: ${baseModule(2.5)}px;
`;

ArtboardDataSizeStyled.displayName = 'ArtboardDataSizeStyled';
