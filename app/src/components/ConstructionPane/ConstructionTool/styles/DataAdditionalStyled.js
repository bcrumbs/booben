'use strict';

import styled from 'styled-components';

import {
  fontSizeSmall,
  textColorMedium,
} from '../../../../styles/themeSelectors';

export const DataAdditionalStyled = styled.div`
  font-size: ${fontSizeSmall}px;
  color: ${textColorMedium};
`;

DataAdditionalStyled.displayName = 'DataAdditionalStyled';
