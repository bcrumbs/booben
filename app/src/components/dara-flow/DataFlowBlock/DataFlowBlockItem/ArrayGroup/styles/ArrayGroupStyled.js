import styled from 'styled-components';
import { baseModule } from '../../../../../../styles/themeSelectors';

export const ArrayGroupStyled = styled.div`
  width: 100%;
  padding: 0 0 ${baseModule(1)}px 0;
`;

ArrayGroupStyled.displayName = 'ArrayGroupStyled';
