/**
 * @author Dmitriy Bizyaev
 */

'use strict';

//noinspection JSUnresolvedVariable
import React, { PureComponent, PropTypes } from 'react';

const propTypes = {
  
};

const defaultProps = {
  
};

export class NewFunctionCodeEditor extends PureComponent {
  constructor(props) {
    super(props);
    
    this.state = {
      
    };
  }
  
  render() {
    
  }
}

NewFunctionCodeEditor.propTypes = propTypes;
NewFunctionCodeEditor.defaultProps = defaultProps;
NewFunctionCodeEditor.displayName = 'NewFunctionCodeEditor';
