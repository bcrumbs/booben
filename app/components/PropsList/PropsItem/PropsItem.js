import React, { PropTypes } from 'react';

export const PropsItem = props => {
    let className = 'props-item';

    return (
        <div className={className}>

        </div>
    );
};

PropsItem.propTypes = {
};

PropsItem.defaultProps = {
};

PropsItem.displayName = 'PropsItem';
